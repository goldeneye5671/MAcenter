import React from 'react'

export default function DateTimeDay({date, setDate, time, setTime, showDay, setDay, setTime, submitClicked, setValidated}) {
    const [errors, setErrors] = React.useState([]);
    return (
        <div>
            
        </div>
    )
}
